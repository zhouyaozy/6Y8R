diff --git a/test/internal/constants.js b/test/internal/constants.js
index 99cc449..90371b7 100644
--- a/test/internal/constants.js
+++ b/test/internal/constants.js
@@ -6,7 +6,10 @@ const constants = require('../../internal/constants')
 t.match(constants, {
   MAX_LENGTH: Number,
   MAX_SAFE_COMPONENT_LENGTH: Number,
+  MAX_SAFE_BUILD_LENGTH: Number,
   MAX_SAFE_INTEGER: Number,
   RELEASE_TYPES: Array,
   SEMVER_SPEC_VERSION: String,
+  FLAG_INCLUDE_PRERELEASE: Number,
+  FLAG_LOOSE: Number,
 }, 'got appropriate data types exported')
diff --git a/test/internal/debug.js b/test/internal/debug.js
index 99d6085..a82f565 100644
--- a/test/internal/debug.js
+++ b/test/internal/debug.js
@@ -3,7 +3,7 @@
 const main = () => {
   const t = require('tap')
   const { spawn } = require('child_process')
-  t.plan(2)
+  t.plan(3)
   t.test('without env set', t => {
     const c = spawn(process.execPath, [__filename, 'child'], { env: {
       ...process.env,
@@ -32,11 +32,27 @@ const main = () => {
       t.end()
     })
   })
+  t.test('with env set multiple args', t => {
+    const c = spawn(process.execPath, [__filename, 'child-multiple'], { env: {
+      ...process.env,
+      NODE_DEBUG: 'semver',
+    } })
+    const err = []
+    c.stderr.on('data', chunk => err.push(chunk))
+    c.on('close', (code, signal) => {
+      t.equal(code, 0, 'success exit status')
+      t.equal(signal, null, 'no signal')
+      t.equal(Buffer.concat(err).toString('utf8'), 'SEMVER hello, world foo\n', 'got expected output for multiple args')
+      t.end()
+    })
+  })
   t.end()
 }
 
 if (process.argv[2] === 'child') {
   require('../../internal/debug')('hello, world')
+} else if (process.argv[2] === 'child-multiple') {
+  require('../../internal/debug')('hello,', 'world', 'foo')
 } else {
   main()
 }
